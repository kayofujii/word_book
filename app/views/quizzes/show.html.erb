<div class="container">
  <div class="row">

    <div class="col-sm-1">
    </div>

    <div class="col-sm-10 text-center">
    <h1>答え</h1>
      <p><%= @quiz.question %></p>
      <p><%= @quiz.right_answer %></p>
      <%= link_to "編集する", edit_quiz_path(@quiz.id) %>
      <%= link_to "削除する", quiz_path(@quiz.id), method: :delete %><br>
    <%= link_to "一覧に戻る", quizzes_pre_path, class:"btn btn-light m-2" %><br>
    <%= link_to "クイズに挑戦", quizzes_path, class:"btn btn-primary m-2" %>
    </div>

    <div class="col-sm-1">
    </div>
  </div>
</div>
